import { MultiDropdown } from './MultiDropdown';
export default {
  title: 'MultiDropdown',
  component: MultiDropdown,
};

export const Template = (arg) => <MultiDropdown {...arg} />;
export const Default = () => Template.bind({});
